# VITE_CHAT_API_URL: Local/remote chat bridge endpoint used by the frontend.
# In local dev, the default is /api/chat and Vite proxies that to http://127.0.0.1:8787.
# Set an absolute URL only when the bridge lives on a different host.
VITE_CHAT_API_URL="/api/chat"
